---
name: shell
description: Run terminal commands in the sandbox for environment setup, building, and running applications.
metadata:
  routable: false
---

This skill provides capabilities for running terminal commands in the sandbox.

## Tools
- `execute_shell`: Run a shell command (install dependencies, start dev server, etc.).
- `check_health`: Verify a dev server is running and accessible.

## Usage
Use this skill for environment setup, building, and running applications.
