# AI Project Instructions

1. Install dependencies with `npm install`.
2. Build the project via `npm run build` before running compiled code.
3. Use `npm start` during development to skip build artifacts.
4. Keep `rules/` documentation in sync with new conventions.
