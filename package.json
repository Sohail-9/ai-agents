{
  "name": "ai-agents",
  "private": true,
  "version": "1.0.0",
  "workspaces": [
    "client",
    "backend"
  ],
  "scripts": {
    "dev": "concurrently -k -n backend,worker,client -c blue,magenta \"npm run dev --prefix backend\" \"npm run worker --prefix backend\" \"npm run dev --prefix client\"",
    "generate": "npm run db:generate --prefix backend",
    "dev:backend": "npm run dev --prefix backend",
    "dev:client": "npm run dev --prefix client",
    "worker": "npm run worker --prefix backend",
    "worker:start": "npm run worker:start --prefix backend",
    "build": "npm run build --prefix backend && npm run build --prefix client",
    "start": "concurrently -k \"npm run start --prefix backend\" \"npm run start --prefix client\"",
    "start:backend": "npm run start --prefix backend",
    "start:client": "npm run start --prefix client",
    "lint": "npm run lint --prefix client"
  },
  "devDependencies": {
    "concurrently": "^9.2.1",
    "dotenv": "^17.3.1"
  },
  "dependencies": {
    "@tavily/core": "^0.7.2"
  }
}
